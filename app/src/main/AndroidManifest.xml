<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.submissiondicoding">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/mobmob_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/mobmob_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SubmissionDicoding">
        <activity android:name=".KontakActivity"></activity>
        <activity android:name=".DetailMobilActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashScreenStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>